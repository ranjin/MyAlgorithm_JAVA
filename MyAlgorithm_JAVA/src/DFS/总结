总结：

当作一棵决策树来看
路径：当前已经做过的选择
选择列表：当前可以做出的选择
结束条件：遍历到树的底层。即选择列表为空的时候

此时引申出前序遍历与后续遍历，因为：
前序遍历是在进入某一个节点之前。
后续遍历是在离开某一个节点之后。

核心框架：
for(选择 in 选择列表) {
	// 做选择
	将该选择从选择列表移除
	路径.add(选择)
	
	backtrack(路径，选择列表)
	// 撤销选择
	路径.remove(选择)
	将该选择再次加入到选择列表
}